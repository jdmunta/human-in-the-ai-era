---
/* A div, not a <p>: MDX passes block-level children, and a <p> inside a <p>
   is invalid HTML — the browser would auto-close the outer one and strip the
   drop cap with it. */
---
<div class="lede"><slot /></div>
