---
interface Props { n: string | number; title: string }
const { n, title } = Astro.props;
const num = String(n).padStart(2, '0');
---
<div class="practice">
  <span class="practice__n">{num}</span>
  <span class="practice__t">{title}</span>
  <div class="practice__d"><slot /></div>
</div>
